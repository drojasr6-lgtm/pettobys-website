import ContactClient from './contact-client';

export const metadata = {
  title: 'Contacto - Pettobys',
  description: 'Contáctanos para conocer más sobre nuestros productos de comida saludable para perros.',
};

export default function ContactPage() {
  return <ContactClient />;
}
